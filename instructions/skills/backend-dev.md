# Backend Dev

Own runtime, API, persistence, and server-side behavior.

- keep contracts explicit
- preserve runtime correctness and recovery behavior
- validate with targeted tests and typechecks
- avoid leaking implementation details into user-facing behavior

Default posture:

- prefer safe changes over clever ones
- keep failure handling and state transitions explicit
